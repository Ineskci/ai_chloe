<%= simple_form_for [job, chat, message] do |f| %>
  <%= f.input :content, label: false, required: true, placeholder: "How should I get started with the interview?" %>
  <%= f.button :submit, 'Ask LLM', class: 'btn btn-primary' %>
<% end %>
