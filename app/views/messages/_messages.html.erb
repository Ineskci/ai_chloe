<%# Loop over messages and display each one %>
<% question_number = 0 %>
<% assistant_count = 0 %>
<% messages.each do |message| %>

  <% if message.role == "user" %>
    <div class="chloe-msg-row user">
      <div class="chloe-msg-sender" style="justify-content: flex-end;">
        You
      </div>
      <div class="chloe-bubble chloe-bubble-user">
        <%= simple_format(ERB::Util.html_escape(message.content)) %>
      </div>
    </div>

  <% elsif message.role == "assistant" %>
    <% assistant_count += 1 %>
    <%# Mensagens 1=saudação, 2=quebra-gelo, 3=resposta quebra-gelo, 4=pergunta1... %>
    <%# Perguntas estão nas posições 4, 6, 8, 10, 12, 14, 16, 18 (pares a partir de 4) %>
    <% is_question = assistant_count >= 4 && assistant_count.even? %>
    <% question_number += 1 if is_question %>
    <div class="chloe-msg-row">
      <div class="chloe-msg-sender">
        Chloé 2.0
      </div>
      <div class="chloe-bubble chloe-bubble-chloe">
        <% if is_question %>
          <strong>Pergunta <%= question_number %>/8</strong>
        <% end %>
        <%= simple_format(ERB::Util.html_escape(message.content)) %>
      </div>
    </div>

  <% end %>
<% end %>
